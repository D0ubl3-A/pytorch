# Matrix-driven binary build workflow for linux-s390x manywheel.
# The list of python / arch combinations is produced at runtime by
# .github/scripts/generate_binary_build_matrix.py --runtime-matrix linux-s390x
# so adding a new python or CPU variant in that script is picked up on the
# next workflow run without regenerating this YAML.
name: linux-s390x-binary-manywheel


on:
  push:
    # NOTE: Meta Employees can trigger new nightlies using: https://fburl.com/trigger_pytorch_nightly_build
    branches:
      - nightly
    tags:
      # NOTE: Binary build pipelines should only get triggered on release candidate builds
      # Release candidate tags look like: v1.11.0-rc1
      - v[0-9]+.[0-9]+.[0-9]+-rc[0-9]+
      - 'ciflow/binaries/*'
      - 'ciflow/binaries_wheel/*'
      - 'ciflow/s390/*'
  workflow_dispatch:

permissions:
  id-token: write

env:
  # Needed for conda builds
  ALPINE_IMAGE: "docker.io/s390x/alpine"
  AWS_DEFAULT_REGION: us-east-1
  BINARY_ENV_FILE: /tmp/env
  BUILD_ENVIRONMENT: linux-s390x-binary-manywheel
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  PR_NUMBER: ${{ github.event.pull_request.number }}
  PYTORCH_FINAL_PACKAGE_DIR: /artifacts
  PYTORCH_ROOT: /pytorch
  SHA1: ${{ github.event.pull_request.head.sha || github.sha }}
  SKIP_ALL_TESTS: 0
concurrency:
  group: linux-s390x-binary-manywheel-${{ github.event.pull_request.number || github.ref_name }}-${{ github.ref_type == 'branch' && github.sha }}-${{ github.event_name == 'workflow_dispatch' }}
  cancel-in-progress: true

jobs:
  get-label-type:
    if: github.repository_owner == 'pytorch'
    name: get-label-type
    uses: pytorch/pytorch/.github/workflows/_runner-determinator.yml@main
    with:
      triggering_actor: ${{ github.triggering_actor }}
      issue_owner: ${{ github.event.pull_request.user.login || github.event.issue.user.login }}
      curr_branch: ${{ github.head_ref || github.ref_name }}
      curr_ref_type: ${{ github.ref_type }}

  generate-matrix:
    if: github.repository_owner == 'pytorch'
    runs-on: ubuntu-latest
    outputs:
      configs: ${{ steps.generate.outputs.configs }}
    steps:
      - name: Checkout PyTorch
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 1
          submodules: false
          show-progress: false
      - name: Generate matrix
        id: generate
        run: python3 .github/scripts/generate_binary_build_matrix.py --runtime-matrix linux-s390x

  build:
    name: ${{ matrix.config.build_name }}-build
    if: ${{ github.repository_owner == 'pytorch' }}
    needs: [get-label-type, generate-matrix]
    strategy:
      fail-fast: false
      matrix:
        config: ${{ fromJSON(needs.generate-matrix.outputs.configs) }}
    uses: ./.github/workflows/_binary-build-linux.yml
    with:
      PYTORCH_ROOT: /pytorch
      PACKAGE_TYPE: manywheel
      # TODO: This is a legacy variable that we eventually want to get rid of in
      #       favor of GPU_ARCH_VERSION
      DESIRED_CUDA: ${{ matrix.config.desired_cuda }}
      GPU_ARCH_TYPE: ${{ matrix.config.gpu_arch_type }}
      DOCKER_IMAGE: ${{ matrix.config.container_image }}
      DOCKER_IMAGE_TAG_PREFIX: ${{ matrix.config.container_image_tag_prefix }}
      DESIRED_PYTHON: ${{ matrix.config.python_version }}
      runs_on: linux.s390x
      ALPINE_IMAGE: "docker.io/s390x/alpine"
      timeout-minutes: 420
      build_name: ${{ matrix.config.build_name }}
      build_environment: linux-s390x-binary-manywheel
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}

  test:
    name: ${{ matrix.config.build_name }}-test
    if: ${{ github.repository_owner == 'pytorch' }}
    needs: [get-label-type, generate-matrix, build]
    strategy:
      fail-fast: false
      matrix:
        config: ${{ fromJSON(needs.generate-matrix.outputs.configs) }}
    uses: ./.github/workflows/_binary-test-linux.yml
    with:
      PYTORCH_ROOT: /pytorch
      PACKAGE_TYPE: manywheel
      # TODO: This is a legacy variable that we eventually want to get rid of in
      #       favor of GPU_ARCH_VERSION
      DESIRED_CUDA: ${{ matrix.config.desired_cuda }}
      GPU_ARCH_TYPE: ${{ matrix.config.gpu_arch_type }}
      DOCKER_IMAGE: ${{ matrix.config.container_image }}
      DOCKER_IMAGE_TAG_PREFIX: ${{ matrix.config.container_image_tag_prefix }}
      DESIRED_PYTHON: ${{ matrix.config.python_version }}
      build_name: ${{ matrix.config.build_name }}
      build_environment: linux-s390x-binary-manywheel
      runs_on: linux.s390x
      ALPINE_IMAGE: "docker.io/s390x/alpine"
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}

  upload:
    name: ${{ matrix.config.build_name }}-upload
    if: ${{ github.repository_owner == 'pytorch' }}
    permissions:
      id-token: write
      contents: read
    needs: [generate-matrix, test]
    strategy:
      fail-fast: false
      matrix:
        config: ${{ fromJSON(needs.generate-matrix.outputs.configs) }}
    with:
      PYTORCH_ROOT: /pytorch
      PACKAGE_TYPE: manywheel
      # TODO: This is a legacy variable that we eventually want to get rid of in
      #       favor of GPU_ARCH_VERSION
      DESIRED_CUDA: ${{ matrix.config.desired_cuda }}
      GPU_ARCH_TYPE: ${{ matrix.config.gpu_arch_type }}
      DOCKER_IMAGE: ${{ matrix.config.container_image }}
      DOCKER_IMAGE_TAG_PREFIX: ${{ matrix.config.container_image_tag_prefix }}
      DESIRED_PYTHON: ${{ matrix.config.python_version }}
      build_name: ${{ matrix.config.build_name }}
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}
      R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
      R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
      R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
    uses: ./.github/workflows/_binary-upload.yml
